# バックルーム生き物観察(仮)

## コンセプト
プレイヤー操作なし・マルチプレイヤーなしの観察型シミュレーション。ランダム生成された入り組んだ部屋+廊下のバックルーム風マップを、SD体型の人型NPCが自律的に歩き回る。行動ループは「探索→発見→(戦う/逃げる/隠れる/手を振って挨拶)を性格で判断→相手が反応」。全員が「アーモンドウォーター」を集めたがっている。プレイヤーは眺める+クリックで個体の詳細を見る。

## 現状(動いているもの)
- 単一HTMLファイル(`backrooms_prototype.html`)。Three.js r128をcdnjs経由のUMDビルドで読み込み、素のJS、ビルド不要、ブラウザで開けばそのまま動く
- GitHub Pagesデプロイ: `.github/workflows/pages.yml` がpush時に `backrooms_prototype.html` を `index.html` としてPagesに公開(リポジトリ設定でPagesのソースを「GitHub Actions」にしておくこと)
- マップ生成: BSP(二分木空間分割)。22x22グリッド・分割深さ5・最小リーフ4で小部屋多数+狭い廊下の入り組んだ構造。フル連結を保証。NPCのスポーン位置は互いに離れた場所を選ぶ(すぐ遭遇しないように)
- 障害物: 床セルの6〜9%に配置。種類は木箱(重み2倍)/黄色い柱/ロッカー/ドラム缶/ソファ/本棚/自販機/観葉植物の8種。配置のたびに床連結性チェック。移動(BFS)と視線(LOS)の両方を遮る
- 見た目: バックルーム風の黄色いストライプ壁紙・カーペット・自販機前面・ドラム缶等をCanvasで手続き生成(外部アセット不要)
- NPC: SD体型の人型(プリミティブ構成、肩・股関節ピボットのアニメーション)。個体値: 名前(カタカナ)、攻撃性、友好度、足の速さ、武器(銃/こぶし)、見た目ランダム
- 視覚: 視野角±66度+距離11+遮蔽物で遮られるLOS。発見すると「!」を出して一瞬固まる
- 発見時の判断: 攻撃性→交戦、友好度→手を振って挨拶(♡エフェクト+腕振りアニメ)、どちらでもなければ逃走。挨拶された側は「挨拶を返す/無視/襲いかかる(攻撃性依存)」で反応する
- 戦闘: 銃撃(射程9、距離命中率、バースト、トレーサー演出)と殴打(チェイス→パンチ)。被弾側はよろけて反撃か逃走を選ぶ(被弾時は反撃寄り)
- 負傷と隠れ: HP35未満で交戦をやめカバーセル(障害物隣接+威嚇者から見えない場所優先)へ走り、しゃがんでHP回復(7/秒)。80%で行動再開
- 追跡判定: 相手が逃走/隠れに移行したとき攻撃側は五分でランダムに追うか見逃すか決める(交戦ごと1回)
- 死亡: HP0で倒れて床に沈み退場。所持アイテムはその場にドロップ。自動補充なし(再生成ボタンで対処)
- アイテム: アーモンドウォーターのボトルがマップに点在(初期10、最大14、10〜18秒ごとに1本リスポーン)。NPCは視界に入ると拾いに行き、所持数が増える。死ぬとドロップ
- インスペクタ: NPCをクリックすると足元に選択リング+右上パネルに名前/HPバー/状態/武器/所持アイテム数/攻撃性/友好度/足の速さがリアルタイム表示。✕か何もない場所クリックで解除(ドラッグとは移動量6pxで区別)
- カメラ: 自動周回+ドラッグ回転+ホイールズーム
- UI: 生き物の数(再生成時)、攻撃性(即時、倍率)、銃を持つ割合(再生成時)、再生成ボタン
- テスト用デバッグフック: `window.__sim`(npcCount / itemCount / carriedTotal / selectFirst)

## 意図的にまだ仮・未実装
- アイテムを巡る駆け引き(所持数の多い個体を狙う、交換など)— 今は「拾う」「死んだら落とす」のみ
- 本物のSD/3Dチビキャラモデル — 現状はプリミティブ組み合わせ(ブルーアーカイブ的な見た目が理想として言及されている)
- 会話・グループ形成などの深い友好インタラクション — 挨拶(手を振る)まで実装済み

## 一緒に検討してほしいこと
- 単一HTMLが1200行超。Vite等のビルドツール+モジュール分割への移行時期
- キャラクターの見た目を本物の3Dモデルに置き換える場合のアセット調達(購入・自作・生成)

## ファイル
- `backrooms_prototype.html` — 現行プロトタイプ本体。ブラウザで開けばそのまま動く
- `.github/workflows/pages.yml` — GitHub Pagesデプロイ用ワークフロー

## 開発メモ
- このサンドボックス環境からはcdnjsに接続できないため、動作確認するときは `npm install three@0.128.0` でローカルにthree.min.jsを取得し、scriptタグの参照をローカルパスに差し替えたテスト用コピーをPlaywright(`/opt/pw-browsers/chromium`)で開く。リポジトリ本体のファイルはCDN参照のまま
