# 夜の学校(仮)— カード探索ボードゲーム

## コンセプト
夕方以降、別世界の教室につながる廊下が現れる学校を舞台にした、ソロのカード探索ボードゲーム(のブラウザ実装)。女子中学生を1人選び、部屋カードを引いてマップを机の上に広げながら夜の校舎を探索し、夜明けまで生き延びれば勝ち。バックルーム的な「際限のない校舎」+〔少女庭国〕(矢部嵩)的な静かな不条理が目標の空気感。詳細ルールと未決事項は `GAME_DESIGN.md` を参照。

※ 以前この リポジトリにあった Three.js 製の観察シミュレーション(backrooms_prototype.html)は、この ボードゲーム企画への方針転換に伴い削除済み(履歴には残っている)。

## 現状(動いているもの)
- 単一HTMLファイル(`nightschool.html`)。**依存ゼロ**(CDNも不要)。素のJS+DOM+CSS。ブラウザで開けばそのまま動く
- GitHub Pagesデプロイ: `.github/workflows/pages.yml` がpush時に `nightschool.html` を `index.html` として公開
- タイトル画面: キャラ4人(真城詩乃/朝倉ひな/白瀬ルカ/黒峰澪、能力値と固有能力に差)+難易度3種(夜の長さ=ターン数)
- コアループ: 1ターン=移動(未探索方向なら部屋カードを引いて配置)/休む/探索 のいずれか。引いたカードは盤面に並び、マップが際限なく広がる(ドラッグでパン、ホイールでズーム)
- 部屋カード11種(廊下/教室/階段/渡り廊下/図書室/保健室/家庭科室/理科室/音楽室/体育館/歪んだ教室)。重み付き抽選で実質無限の山札。入ってきた側に必ず扉が生える(校舎が組み変わる設定でカード回転を省略)
- 到着イベント: 何もない(フレーバー)/アイテム/謎の存在/生徒との遭遇/環境ハザード。部屋種と時間帯で重みが変わる
- 判定: 2d6+能力値 ≥ 目標値。遭遇時は 戦う(腕力)/逃げる(敏捷)/隠れる(直感) から選択。敵6種(一部は戦えない・逃げられない)
- 時間帯: 宵→深夜→丑三つ時→夜明け前。深い時間ほど遭遇率と被害が増える。丑三つ時は3ターンごとに気力-1(ルカ無効)
- リソース: 体力(0で死亡END)/気力(0で「夜の学校の生徒になる」END)。勝利は夜明けまで生存
- 持ち物(上限4): 装備3種(能力+1)/消耗品(救急セット/チョコ/塩=遭遇回避/カメラ=次の部屋を先見)/お守り(失敗を1回自動で無効)
- ログパネルにダイス目・イベントを全記録。少女庭国風のフレーバーテキスト
- テスト用デバッグフック: `window.__game`(newGame/state/legalMoves/move/rest/search/encounterOptions/encounterAct/useItem)

## バランスの現状値
ランダム操作ボットで12戦9勝3敗程度(Playwright自動テスト)。人間が考えて遊べばもう少し勝てるはず。判定目標値7〜9、遭遇失敗で体力/気力-1〜2(丑三つ時以降+1)。

## 意図的にまだ仮・未実装(GAME_DESIGN.md の未決事項も参照)
- 脱出条件は「生存のみ」(昇降口へ戻る要求はなし)
- 生徒NPCの同行者化、「最後に1人」系の少女庭国オマージュ要素
- 手札あふれ時の入れ替え選択、シード共有、セーブ
- キャラのイラスト表示(現在はテキストのみ。デザイン画像は用意済みの想定)

## ファイル
- `nightschool.html` — ゲーム本体
- `GAME_DESIGN.md` — ルール設計と未決事項リスト
- `.github/workflows/pages.yml` — GitHub Pagesデプロイ(nightschool.html → index.html)

## 開発メモ
- 依存ゼロなのでサンドボックスでもそのまま動作確認できる: ローカルHTTPサーバで配信してPlaywright(`/opt/pw-browsers/chromium`)で開く
- ロジック検証は `window.__game` フックでランダムプレイを回すのが手軽(勝率・エラー有無を見る)
- `<script>`部分だけ抜き出して `node --check` すると構文エラーを早期に拾える
