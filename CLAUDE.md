# バックルーム生き物観察(仮)

## コンセプト
プレイヤー操作なし・マルチプレイヤーなしの観察型シミュレーション。ランダム生成された部屋+廊下のバックルーム風マップを、複数のかわいいNPCが自律的に経路探索しながら歩き回る。NPC同士が接近すると「戦う/逃げる/話す」のいずれかがランダムに発生する。プレイヤーは基本的に眺めるだけ。

## 現状(動いているもの)
- 単一HTMLファイル(`backrooms_prototype.html`)。Three.js r128をcdnjs経由のUMDビルドで読み込み、素のJS、ビルド不要、ブラウザで開けばそのまま動く
- マップ生成: BSP(二分木空間分割)で部屋を配置し、隣接ノード同士をL字廊下で接続。16x16グリッド、セルサイズ2.4。毎回フル連結を保証する設計(300回のランダム生成 + 1000回のパスファインディングをNode.jsで自動テスト済み、全て通過)
- 経路探索: グリッド上のBFS。NPCがidle状態を抜けるとき、床セルの中からランダムに目的地を選び、ウェイポイント列に変換して追従する
- NPC: 卵形の球体本体+目+ランダムでアンテナ、というプレースホルダーの見た目。idle/walking/fleeing/encounterの4状態を持つ。歩行中はバウンス+スクワッシュ&ストレッチのアニメーションが付く
- エンカウンター: 2体が半径1.1以内に接近すると、戦う/逃げる/話すの重み付き抽選が発生する(重みはUIスライダーで即時調整可能)。それぞれに専用の一時エフェクト(戦う=赤いトゲ、逃げる=灰色の三角、話す=青いリング)が付く。3秒のクールダウンあり
- カメラ: 自動でゆっくり周回 + ドラッグで手動回転 + ホイールでズーム
- UI: 生き物の数(「マップを作り直す」を押した時に反映)、戦う/逃げる/話すの重み(即時反映)、マップ再生成ボタン

## 意図的にまだ仮・未実装
- NPCのランダム生成(名前・能力値・装備・アイテム)— 現状は色のバリエーション以外ほぼ無個性
- 持ち帰りたいアイテム/探索目標 — 部屋の構造自体はあるので、後から足す土台にはなるはず
- 「戦う」の実際の勝敗・結果 — 今は視覚的な演出のみ。能力値システムがないので実際には何も起きない
- 本物のSD/3Dチビキャラモデル — 現状は仮の球体ジオメトリ(ブルーアーカイブ的な見た目が理想として言及されている)

## 一緒に検討してほしいこと
- このままシングルHTMLファイルで機能を足し続けるか、早めにちゃんとしたプロジェクト構成(Vite等のビルドツール + モジュール分割)に移行するか。NPCの個体データやアイテムシステムが増えていくと、素のHTML1枚だと管理がつらくなりそうな段階に来ている
- キャラクターの見た目を本物の3Dモデルに置き換える場合、アセットをどう調達するか(購入・自作・生成)
- 「戦う」に実際の勝敗を持たせるなら、能力値システムとセットで設計する必要がある

## ファイル
- `backrooms_prototype.html` — 現行プロトタイプ本体。まずはこれをそのまま動かして感触を確認してから着手するのがおすすめ
