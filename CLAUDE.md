# バックルーム生き物観察(仮)

## コンセプト
プレイヤー操作なし・マルチプレイヤーなしの観察型シミュレーション。ランダム生成された部屋+廊下のバックルーム風マップを、SD体型の人型NPCが自律的に歩き回る。NPCは視覚を持ち、他のNPCを見つけると攻撃(銃撃/殴る)するか逃げるかを性格(攻撃性)に応じて決める。プレイヤーは基本的に眺めるだけ。

## 現状(動いているもの)
- 単一HTMLファイル(`backrooms_prototype.html`)。Three.js r128をcdnjs経由のUMDビルドで読み込み、素のJS、ビルド不要、ブラウザで開けばそのまま動く
- GitHub Pagesデプロイ: `.github/workflows/pages.yml` がpush時に `backrooms_prototype.html` を `index.html` としてPagesに公開する(リポジトリ設定でPagesのソースを「GitHub Actions」にしておくこと)
- マップ生成: BSP(二分木空間分割)で部屋を配置し、隣接ノード同士をL字廊下で接続。16x16グリッド、セルサイズ2.4。フル連結を保証
- 障害物: 床セルの7〜11%に木箱/黄色い柱/ロッカーをランダム配置。配置のたびに床の連結性をチェックするので通行不能にはならない。障害物は移動(BFS経路探索)と視線(LOS)の両方を遮る
- 見た目: バックルーム風の黄色いストライプ壁紙とカーペットをCanvasで手続き生成してテクスチャ化(外部アセット不要)
- NPC: SD体型(2.5頭身くらい)の人型。頭・胴・腕・脚をプリミティブで構成し、肩・股関節ピボットで歩行スイングアニメーション。肌/服/髪色はランダム。銃持ちは右手に銃モデル
- 視覚: 視野角±66度+距離11+遮蔽物・壁で遮られる視線判定(セグメントサンプリング)。徘徊中に他NPCが見えると「!」を出して0.3秒ほど固まり、攻撃性に応じて交戦か逃走を選ぶ
- 戦闘: 銃撃(射程9、距離で命中率変動、バースト射撃、トレーサー+マズルフラッシュ演出)と殴打(接近チェイス→振りかぶり→パンチ)の2種。武器はスポーン時に決まる(スライダーで割合調整)
- 被弾リアクション: 撃たれた/殴られたNPCはよろけた後、反撃するか逃げるかを選ぶ(被弾時は反撃寄りに補正)。HPが0になると倒れて5〜9秒ダウン→HP半分で起き上がり弱体化状態(移動速度低下)で逃走。消滅はしない
- カメラ: 自動でゆっくり周回 + ドラッグで手動回転 + ホイールでズーム
- UI: 生き物の数(マップ再生成時に反映)、攻撃性(即時反映、全NPCの性格の倍率)、銃を持つ割合(再生成時に反映)、マップ再生成ボタン

## 意図的にまだ仮・未実装
- NPCのランダム生成の深掘り(名前・能力値の個体差表示・装備・アイテム)— HP100固定で、個体差は攻撃性・移動速度・見た目のみ
- 持ち帰りたいアイテム/探索目標
- 本物のSD/3Dチビキャラモデル — 現状はプリミティブ組み合わせの手作りモデル(ブルーアーカイブ的な見た目が理想として言及されている)
- 「話す」などの友好的なインタラクション(視覚ベースに作り替えた際にいったん外した)

## 一緒に検討してほしいこと
- このままシングルHTMLファイルで機能を足し続けるか、Vite等のビルドツール+モジュール分割に移行するか。状態機械が9状態まで増えており、そろそろ分割の検討時期
- キャラクターの見た目を本物の3Dモデルに置き換える場合、アセットをどう調達するか(購入・自作・生成)

## ファイル
- `backrooms_prototype.html` — 現行プロトタイプ本体。ブラウザで開けばそのまま動く
- `.github/workflows/pages.yml` — GitHub Pagesデプロイ用ワークフロー

## 開発メモ
- このサンドボックス環境からはcdnjsに接続できないため、動作確認するときは `npm install three@0.128.0` でローカルにthree.min.jsを取得し、scriptタグの参照をローカルパスに差し替えたテスト用コピーをPlaywright(`/opt/pw-browsers/chromium`)で開く。リポジトリ本体のファイルはCDN参照のまま
